# 🚀 JANDALISYS - نظام إدارة المؤسسة

نظام متكامل لإدارة التدفق النقدي والعقود والمعاملات المالية.

## 🐳 استخدام Docker (الطريقة الموصى بها)

يوفر Docker بيئة معزولة تضمن عمل النظام بنفس الطريقة على أي جهاز.

### 1. المتطلبات
- تثبيت [Docker Desktop](https://www.docker.com/products/docker-desktop/) على ويندوز.

### 2. التشغيل
افتح PowerShell في مجلد المشروع وقم بتشغيل:
```powershell
docker-compose up --build -d
```
- سيعمل الـ **Backend** على: `http://localhost:8000`
- سيعمل الـ **Frontend** على: `http://localhost` (منفذ 80)

### 3. إدارة البيانات في Docker
- يتم حفظ قاعدة البيانات في مجلد `./server/data`.
- يتم حفظ الملفات المؤرشفة في مجلد `./storage`.

---

## 🛠️ استخدام Git لإدارة المشروع ونقله

إذا كنت ترغب في رفع المشروع على Git (مثل GitHub أو GitLab) لنقله بسهولة:

### 1. إعداد المستودع (Repository)
```powershell
# تهيئة Git في المجلد
git init

# إضافة كافة الملفات (سيتم استثناء الملفات الكبيرة والمؤقتة تلقائياً عبر .gitignore)
git add .

# تسجيل أول نسخة (Commit)
git commit -m "Initial commit with Docker configuration"
```

### 2. الرفع إلى خادم بعيد (Remote)
بعد إنشاء مستودع جديد على GitHub:
```powershell
git remote add origin https://github.com/your-username/your-repo-name.git
git branch -M main
git push -u origin main
```

---

## 📋 البدء السريع (بدون Docker)

## 🔧 متطلبات النظام

### الحد الأدنى
- Windows 10/11 (64-bit)
- Python 3.9+
- Node.js 18+
- 4GB RAM
- 20GB### المُوصى مساحة فارغة

 به
- Windows Server 2019/2022
- 8GB+ RAM
- 50GB+ SSD
- اتصال إنترنت مستقر

## 🛡️ الأمان

- تغيير المفاتيح السرية في الإنتاج
- تفعيل HTTPS مع Let's Encrypt
- إعداد جدار الحماية
- نسخ احتياطية دورية
- مراقبة السجلات

## 📊 الميزات

### إدارة العقود
- إنشاء وتحرير العقود
- تتبع حالة العقود
- إدارة البضائع والأسعار
- تقارير مالية

### إدارة الحضور
- تسجيل أوقات الحضور
- إدارة الورديات
- تتبع التأخير والغياب
- تقارير الحضور

### لوحة التحكم
- مؤشرات الأداء الرئيسية
- رسوم بيانية تفاعلية
- إشعارات فورية
- واجهة مستخدم حديثة

## 🆘 الدعم

- **الوثائق**: راجع الملفات المرفقة
- **استكشاف الأخطاء**: راجع قسم troubleshooting في الدليل الشامل
- **المشاكل الشائعة**: ملف استكشاف الأخطاء في الدليل

## 🎯 خطوات سريعة للاختبار

```powershell
# 1. تثبيت المتطلبات
install_windows.bat

# 2. تشغيل التطبيق
cd server && .\venv\Scripts\Activate.ps1 && uvicorn main:app --reload
cd client && npm run dev

# 3. فتح المتصفح
# http://localhost:5173
```

## 📈 الإنتاج

للنشر في الإنتاج:
1. إعداد VPS أو خدمة سحابية
2. تكوين قاعدة بيانات PostgreSQL
3. إعداد Nginx كعكس وكيل
4. تفعيل SSL مع Let's Encrypt
5. إعداد مراقبة النظام والنسخ الاحتياطية

---

**🎉 مبروك! نظام إدارة التدفق النقدي جاهز للاستخدام**

*تذكر: الأمان أولاً، النسخ الاحتياطية ضرورية، المراقبة الدورية تضمن الاستقرار*